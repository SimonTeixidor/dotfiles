#!/bin/bash
xrandr | grep 3840 >/dev/null && xrdb -merge <(echo 'Xft.dpi: 120')
picom -c -b --experimental-backends &
hsetroot -cover .local/share/wall.jpg &
prowler &
redshift -l 47:8 &
$(sleep 1; ~/bin/status) &
while true; do
	dwm
done
